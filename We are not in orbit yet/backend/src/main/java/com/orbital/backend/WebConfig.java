package com.orbital.backend;

import org.springframework.context.annotation.Configuration;

/**
 * CORS is now configured in SecurityConfig.java
 * This file can be kept for future web configurations if needed
 */
@Configuration
public class WebConfig {
    // CORS configuration moved to SecurityConfig for better security integration
}
